server:
  port: 8080

spring:
  application:
    name: restaurant
  datasource:
    url: jdbc:postgresql://localhost:5432/localhost
    username: admin
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

